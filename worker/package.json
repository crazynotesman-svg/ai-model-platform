{
  "name": "@amp/worker",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers API backend for the AI Model Intelligence Platform.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^6",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1"
  }
}
